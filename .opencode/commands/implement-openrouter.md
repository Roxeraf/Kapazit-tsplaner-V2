---
description: Run the full engineering pipeline entirely through OpenRouter
agent: orchestrator-openrouter
---

Implement the following requirement using the complete OpenRouter engineering
workflow:

$ARGUMENTS

Use:

EXPLORE
→ PLAN
→ BUILD
→ VERIFY
→ REVIEW
→ COMPLETE

Use only OpenRouter agents.

Apply normal cost controls.

Use Kimi expert only after the configured normal fix loop fails.

Do not skip mandatory phases.